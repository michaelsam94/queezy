import 'package:flutter/material.dart';

class SearchQuizSection extends StatelessWidget {
  const SearchQuizSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('quiz'),
    );
  }
}
