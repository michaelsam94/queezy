import 'package:flutter/material.dart';

class SearchFriendsSection extends StatelessWidget {
  const SearchFriendsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('friends'),
    );
  }
}
